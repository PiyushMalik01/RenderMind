# Core dependencies
websockets>=15.0.1
requests>=2.32.3

# Model serving (Flask + ML)
flask>=3.0.0
flask-cors>=4.0.0
transformers>=4.30.0
torch>=2.0.0
peft>=0.4.0
accelerate>=0.20.0
bitsandbytes>=0.41.0

# Optional: for better performance
sentencepiece>=0.1.99
protobuf>=3.20.0
